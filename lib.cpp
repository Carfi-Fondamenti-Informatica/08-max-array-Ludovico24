#Includi "lib.h"
float maxArray(float array[], int d){
    for(int i = 0; i < d; ++i){
        if(array[0] > array[i]){

        }
        altro{
            array[0] = array[i];
        }
    }
    matrice restituita[0];
}
